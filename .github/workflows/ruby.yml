# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will download a prebuilt Ruby version, install dependencies and run tests with Rake
# For more information see: https://github.com/marketplace/actions/setup-ruby-jruby-and-truffleruby

name: Ruby

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

งาน: Account ID

It will help you with quick contact with our support staff.

1981-149-97

    ID: 322355129176850

 Page ID: 100208301363325

 Access token: EAAT5PBsEsRIBAOlTMBLUQPi85OdAwOZBMhDIwxn2nVaVzyqrb ...

 Download sample code
  ทดสอบ:
  คอนเทนเนอร์: teruxs.atlassian.net

    runs-on: ubuntu-latest

    steps: Pouri88  

# Step 1: Authentication

 $ Cat ~ / GH_TOKEN.txt | docker login docker.pkg.github.com -u tex 101 -password -stdin

#Step 2: tag

 $ Docker tag IMAGE_ID docker.pkg.github.com/101/ eth hub / IMAGE_NAME: version

 #Step 3: Publish

 $ Docker.pkg.github.com/101/ eth hub / IMAGE_NAME: version
    - uses: actions/checkout@v2
    - name: Set up Ruby
    # To automatically get bug fixes and new Ruby versions for ruby/setup-ruby,
    # change this to (see https://github.com/ruby/setup-ruby#versioning):
    # uses: ruby/setup-ruby@v1
      uses: ruby/setup-ruby@ec106b438a1ff6ff109590de34ddc62c540232e0
      with:
        ruby-version: 2.6
    - name: Install dependencies
      run: bundle install
    - name: Run tests
      run: bundle exec rake
